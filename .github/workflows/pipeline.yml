name: Data Pipeline CI

on:
  push:
    branches:
      - main

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t data-pipeline .

      - name: Run pipeline
        run: docker run data-pipeline